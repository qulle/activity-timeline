import { Position } from "../models/position.model";

/**
 * Default object containing position data
 */
const DefaultPosition: Position = {
    left: 0,
    top: 0,
    x: 0,
    y: 0
};

export { DefaultPosition };
