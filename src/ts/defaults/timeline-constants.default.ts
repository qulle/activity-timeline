import { TimelineConstants } from "../models/timeline-constants.model";

/**
 * This data can't be overridden from the JSON-files
 */
const TimelineConstants: TimelineConstants = {
    radius: 18,
    amplification: 2,
    stepDistanceXAxis: 150,
    stepDistanceYAxis: 80,
    canvasInternalPadding: 20,
    xPadding: 150,
    yPadding: 80
};

export { TimelineConstants };
